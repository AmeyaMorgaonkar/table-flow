# Documentation Rules

Always on.

- JSDoc on all exported functions — params, return type, brief description
- Update README when any behavior, route, or schema changes
- Add CHANGELOG entry for every feature and bugfix
- Complex business logic (OTP flow, session lifecycle) must have inline comments
- Schema changes must be documented in docs/schema.md
